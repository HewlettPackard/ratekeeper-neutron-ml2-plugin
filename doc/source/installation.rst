============
Installation
============

At the command line::

    $ pip install networking-hp

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-hp
    $ pip install networking-hp

