[ratekeeper]
# (IntOpt) Default minimum bandwidth rate in mbps to apply to the ports attached to a netowrk
# default_min_rate =

# IntOpt) Default maximun bandwidth rate in mbps to apply to the ports attached to a netowrk
# default_max_rate =


[ratekeeper_agent]
# (IntOpt) The number of seconds the agent will wait between polling for local device changes
# polling_interval =
